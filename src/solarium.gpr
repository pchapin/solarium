project Solarium is

   for Object_Dir use "build";
   for Main use ("solarium.adb");

   package Compiler is
      for Default_Switches ("ada") use ("-gnato", "-fstack-check", "-gnat12", "-gnata", "-O2");
   end Compiler;

end Solarium;

